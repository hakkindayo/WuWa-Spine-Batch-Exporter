# WuWa-2D-Animation-Spine-Viewer

FModelでエクスポートされたJSONファイル（SpineAtlasAsset）から、鳴潮（Wuthering Waves）のアセット向けにSpineアニメーション（PNG連番・動画）を自動で抽出し、復元・一括書き出しするPythonスクリプトです。

Playwright（ヘッドレスChromium）を使用してブラウザをバックグラウンドで自動操作するため、手動でのブラウザ操作は一切不要です。

---

## 主な機能

1. **アセットの復元:**
   - キャラクターフォルダ以下のFModel書き出し済みJSONから `.skel` および `.atlas` ファイルを復元します。
   - 対応するPNGテクスチャとまとめ、出力ディレクトリ内のキャラクター別フォルダに整理します。
2. **ヘッドレス環境での決定論的レンダリング:**
   - `spine-webgl` を用いて、各キャラクターのすべてのアニメーションを1フレームずつ正確にレンダリングします。
3. **デュアル動画出力（FFmpeg経由）:**
   - **通常再生用:** `<アニメ名>.mp4` （不透明）
   - **背景透過用:** `<アニメ名>_alpha.mov` （ロスレスPNGコーデック ＋ アルファチャンネル対応）
4. **自動クリーンアップ:**
   - 変換成功後、中間生成された連番PNGや抽出済みの生アセットを自動で削除します（FFmpegが失敗した場合のみ、フォールバックとして連番PNGが保持されます）。
5. **設定の永続化:**
   - 読み込み元および保存先のパスは、同じディレクトリ内の `wuwa_config.json` に自動保存されます。

---

## 事前準備

コマンドプロンプトを開き、初回のみ以下のコマンドを実行してください：

```bash
pip install playwright numpy pillow
playwright install chromium
```
